# Лабораторные работы по предмету: "Параллельное программирование"
> Студент группы 932223 - **Антропов Илья Александрович** \
> 4 курс 2 семестр 

## 1. Двухточечные обмены
> На процессе с номером 0 заполнить массив x_global[] случайными числами из
диапазона от 0 до 9. Размер массива задать равным n = 100. Используя функции
двухточечного обмена переслать вектор x_global[] по кольцу. При получении вектора каждый процесс должен перебрать элементы массива с номерами кратными номеру процесса и вычислить количество четных чисел среди них. Полученное значение четных чисел каждый процесс пересылает на процесс с номером ноль. Нулевой процесс в слою очередь формирует из полученныхэлементов массив even_number[], где на месте i-го элемента храниться количество четных чисел на i-м процессе.
- lab1.cpp
## 2. Коллективные операции
> На процессе номер ноль заполнить матрицу A_global[n][m] случайными числами из диапазона от -9 до 9. Размер массива задать равным n = 4*size, m = 5, где size число используемых процессов. Распределить матрицу A_global[][] по процессам. Определить глобальный максимальный элемент матрицы среди всех элементов. Выдать результат на процессе с номером 1.
- lab2.cpp
## 3. Распараллеливание матричных алгоритмов 
> Реализовать параллельный алгоритм вычисления нормы матрицы A[][]. Матрицу заполнить случайными числами из диапазона от -9 до 9. Размер массива задать равным n = 4 * size, m=4, где size число используемых процессов. Матрицу распределить по процессам по строкам. Результат получить на каждом процессе.
- lab3.cpp
## 4. Алгоритмы суммирования
> Написать параллельную MPI программу вычисления скалярного произведение двух
векторов X и Y длиной n=10^6-10^7. Для удобства отладки, пусть оба вектора состоят из [1]. Требуется распределить вектора между процессорами, а затем, в зависимости от варианта, реализовать алгоритм каскадную схему суммирования. Формула для скалярного произведения векторов: 𝑺 = ∑ 𝒙_𝒊∙ 𝒚_𝒊 n to i=1
- lab4.cpp
## 5. Вычисление определенных интегралов
> Написать параллельную MPI программу приближенного вычисления определенного
интеграла ∫ 𝑓(𝑥)𝑑𝑥 b to a c большой точностью. Число разбиений взять равным 𝑛 = 10^7 или больше. Вид обобщенной формулы численного интегрирования метод средних прямоугольников. Вид функции 𝑓(𝑥) f(x)=1/(√x(exp(0.9x)+3)), a=0.5, b=2. Оценить ускорение и эффективность параллельной программы. 
- lab5.cpp
## 6. Вычисление кратных интегралов
> Написать параллельную MPI программу приближенного вычисления двойного интеграла ∬ 𝑓(𝑥, 𝑦)𝑑𝑥𝑑𝑦 to 𝐷 c большой точностью. Число разбиений взять равным 𝑛 = 10^7 или больше. Вид обобщенной формулы численного интегрирования метод сеток. Вид функции 𝑓(𝑥) f(x,y)=|xy|, D={x^4+y^4<1}.  Оценить ускорение и эффективность параллельной программы.
- lab6.cpp
## 7. Параллелизация алгоритма умножения матриц (OpenMP) 
> Построить параллельный алгоритм умножения матриц: Исследовать время счета, ускорение и эффективность OpenMP программы умножения матриц для вариантов циклов i-k-j и k-i-j. С помощью ключей dynamic для директивы for найти распределение итераций по нитям с минимальным временем выполнения. Оценку времени работы программы проводить минимум по трем ее запускам.
- lab7.cpp
- find_chunk.sh
## 8. Вычисление определенных интегралов (OpenMP) 
> Написать параллельную OpenMP программу приближенного вычисления определенного интеграла ∫ 𝑓(𝑥)𝑑𝑥 b to 𝑎 c большой точностью. Число разбиений взять равным 𝑛 = 10^7 или больше. Вид обобщенной формулы численного интегрирования метод симпсона. Вид функции 𝑓(𝑥) f(x)=(x^2+sin(0.48(x+2)))/(exp(x^2)+0.38), a=0.4, b=1. Оценить ускорение и эффективность параллельной программы.
- lab8.cpp
